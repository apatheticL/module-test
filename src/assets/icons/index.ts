import type {ImageStyle, StyleProp} from 'react-native';
import {Icon, IconElement, IconSource} from './icon.component';
export {Icon, RemoteIcon} from './icon.component';
export type {IconSource} from './icon.component';

export const ArrowLeftIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arrow-left.png'),
  };

  return Icon(source, style);
};

export const MessageIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/message-icon.png'),
  };

  return Icon(source, style);
};
export const AddToCartIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/add-to-cart-icon.png'),
  };

  return Icon(source, style);
};
export const ArrowDownIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arrow-down.png'),
  };

  return Icon(source, style);
};

export const ArrowUpIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arrow-up.png'),
  };

  return Icon(source, style);
};

export const LeftArrowIcon2 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/left-arrow-2.png'),
  };

  return Icon(source, style);
};

export const LocationIcon1 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/location-icon-1.png'),
  };

  return Icon(source, style);
};

export const ModalLine = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/modal-line.png'),
  };

  return Icon(source, style);
};

export const CardIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/card-icon.png'),
  };

  return Icon(source, style);
};

export const CartIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/cart.png'),
  };

  return Icon(source, style);
};

export const Home = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/card-icon.png'),
  };

  return Icon(source, style);
};

export const Search2Icon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/search-2.png'),
  };

  return Icon(source, style);
};

export const CheckboxIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/checkbox.png'),
  };

  return Icon(source, style);
};

export const CheckedboxIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/checkedbox.png'),
  };

  return Icon(source, style);
};
export const ArrowRight2Icon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arrow-right-2.png'),
  };

  return Icon(source, style);
};
export const EditIcon3 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/edit-icon-3.png'),
  };

  return Icon(source, style);
};
export const LocationIcon2 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/location-icon-2.png'),
  };

  return Icon(source, style);
};

export const CloseCircleIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/close-circle.png'),
  };

  return Icon(source, style);
};
export const Close2Icon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/close-2.png'),
  };

  return Icon(source, style);
};
export const DeleteIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/icon-delete.png'),
  };

  return Icon(source, style);
};

export const CloseIcon3 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/close-icon-3.png'),
  };

  return Icon(source, style);
};

export const AdditionIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/icon-addition.png'),
  };

  return Icon(source, style);
};

export const SubtractionIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/icon-subtraction.png'),
  };

  return Icon(source, style);
};
export const StarIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/star-icon.png'),
  };
  return Icon(source, style);
};
export const CloseBgIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/icon-close.png'),
  };

  return Icon(source, style);
};

export const BookingListIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/booking-list.png'),
  };

  return Icon(source, style);
};
export const DiscountIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/discount-icon.png'),
  };
  return Icon(source, style);
};
export const CartBdIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/icon-cart-1.png'),
  };

  return Icon(source, style);
};

export const MyCartIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/my-cart.png'),
  };

  return Icon(source, style);
};
export const ArrowLeft3Icon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/arrow-left-3.png'),
  };
  return Icon(source, style);
};
export const SuccessIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/success.png'),
  };

  return Icon(source, style);
};

export const SearchV3Icon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/search-icon-v3.png'),
  };

  return Icon(source, style);
};
export const FailIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Fail.png'),
  };

  return Icon(source, style);
};

export const FlashSaleIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/flash-sale-icon.png'),
  };

  return Icon(source, style);
};
export const EmptyIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Emty.png'),
  };

  return Icon(source, style);
};

export const ProductsIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/products-icon.png'),
  };

  return Icon(source, style);
};
export const WaitingIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/waiting.png'),
  };

  return Icon(source, style);
};

export const TimeSale = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/time-sale-icon.png'),
  };

  return Icon(source, style);
};
export const OngoingdeliveriesIcon = (
  style: StyleProp<ImageStyle>,
): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Ongoingdeliveries.png'),
  };

  return Icon(source, style);
};
export const SearchIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/search-icon.png'),
  };

  return Icon(source, style);
};
export const ClockIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/clock-icon.png'),
  };

  return Icon(source, style);
};
export const BagIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/bag-icon.png'),
  };
  return Icon(source, style);
};
export const BackIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/back.png'),
  };

  return Icon(source, style);
};

export const VoucherIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/voucher-icon.png'),
  };

  return Icon(source, style);
};
export const LightningIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/lightning-icon.png'),
  };

  return Icon(source, style);
};
export const CloseIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Close.png'),
  };

  return Icon(source, style);
};
export const ShieldIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/shield-icon.png'),
  };

  return Icon(source, style);
};

export const FilterIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/Filter.png'),
  };

  return Icon(source, style);
};

export const ChatIcon = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/chat-icon.png'),
  };
  return Icon(source, style);
};

export const ArrowLeftIcon2 = (style: StyleProp<ImageStyle>): IconElement => {
  const source: IconSource = {
    imageSource: require('./source/left-arrow.png'),
  };

  return Icon(source, style);
};
