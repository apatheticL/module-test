export enum ProductTypeEnum {
  Products = 1,
  Services,
}
export enum LoadDataStatusEnum {
  LoadNew = 'LoadNew',
  LoadMore = 'LoadMore',
}
export enum SearchTypeEnum {
  Products = 1,
  Services,
  All,
}
